{
  "name": "lockerdome-perf-test",
  "version": "0.0.1",
  "scripts": {
    "start": "node init;",
    "install": "pwd;cd extensions/binary; node-gyp configure; node-gyp build; cp ./build/Release/binary.node ../.."
  }
}
